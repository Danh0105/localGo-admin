import { describe,expect,it } from 'vitest'; import type { CraftVillageAdminItem } from '../../../../types/craft-village'; import { craftVillageFormSchema,craftVillageToForm,formToCraftVillagePayload } from '../craft-village.schema';
const item:CraftVillageAdminItem={id:'pottery',version:2,name:'Làng gốm LocalGo',category:'Thủ công truyền thống',address:'Bình Dương',workingTime:'08:00 - 17:00',mainProducts:'Gốm thủ công',summary:'Làng nghề truyền thống',description:['Đoạn giới thiệu'],highlights:['Nghệ nhân lâu năm'],visitorNote:'Đặt lịch trước',mediaId:'media-1',imageUrl:'https://cdn/pottery.webp',imageAlt:'Nghệ nhân làm gốm',sortOrder:0,isActive:true,updatedAt:'2026-07-15T00:00:00.000Z',updatedBy:null};
describe('Craft village schema and mapper',()=>{
  it('maps lists and trims payload without losing valid line breaks',()=>{const payload=formToCraftVillagePayload({...craftVillageToForm(item),name:'  Tên mới  ',descriptions:[{text:'  Dòng 1\nDòng 2  '}]});expect(payload.name).toBe('Tên mới');expect(payload.description).toEqual(['Dòng 1\nDòng 2']);expect(payload.mediaId).toBe('media-1');});
  it('requires core fields, image and a non-empty description',()=>{const result=craftVillageFormSchema.safeParse({...craftVillageToForm(item),name:'',address:'',workingTime:'',mainProducts:'',summary:'',mediaId:null,imageUrl:null,descriptions:[{text:' '}]});expect(result.success).toBe(false);if(!result.success)expect(result.error.issues.map((issue)=>issue.path.join('.'))).toEqual(expect.arrayContaining(['name','address','workingTime','mainProducts','summary','imageUrl','descriptions.0.text']));});
  it('limits descriptions and highlights to 20',()=>{const form=craftVillageToForm(item);expect(craftVillageFormSchema.safeParse({...form,descriptions:Array.from({length:21},()=>({text:'Đoạn'}))}).success).toBe(false);expect(craftVillageFormSchema.safeParse({...form,highlights:Array.from({length:21},()=>({text:'Điểm'}))}).success).toBe(false);});
});
