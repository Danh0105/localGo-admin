import{describe,expect,it}from'vitest';import type{FestivalAdminItem}from'../../../../types/festival';import{festivalFormSchema,festivalToForm,formToFestivalPayload}from'../festival.schema';const item:FestivalAdminItem={id:'spring',version:2,name:'Lễ hội mùa xuân',category:'Lễ truyền thống',time:'Mùng 10 tháng Giêng',location:'Đình làng',scale:'Cấp xã',summary:'Lễ hội truyền thống',description:['Đoạn giới thiệu'],activities:['Rước lễ'],note:'Giữ trật tự',mediaId:'media-1',imageUrl:'https://cdn/festival.webp',imageAlt:'Đoàn rước',sortOrder:0,isActive:true,updatedAt:'2026-07-15T00:00:00.000Z',updatedBy:null};describe('Festival schema and mapper',()=>{it('maps arrays and trims payload',()=>{const payload=formToFestivalPayload({...festivalToForm(item),name:'  Lễ hội mới  ',descriptions:[{text:'  Dòng 1\nDòng 2  '}],activities:[{text:'  Múa lân  '}]});expect(payload.name).toBe('Lễ hội mới');expect(payload.description).toEqual(['Dòng 1\nDòng 2']);expect(payload.activities).toEqual(['Múa lân']);});it('requires core fields, image and description',()=>{const result=festivalFormSchema.safeParse({...festivalToForm(item),name:'',time:'',location:'',scale:'',summary:'',mediaId:null,imageUrl:null,descriptions:[{text:' '}]});expect(result.success).toBe(false);if(!result.success)expect(result.error.issues.map((issue)=>issue.path.join('.'))).toEqual(expect.arrayContaining(['name','time','location','scale','summary','imageUrl','descriptions.0.text']));});it('limits dynamic lists to 20',()=>{const form=festivalToForm(item);expect(festivalFormSchema.safeParse({...form,descriptions:Array.from({length:21},()=>({text:'Đoạn'}))}).success).toBe(false);expect(festivalFormSchema.safeParse({...form,activities:Array.from({length:21},()=>({text:'Hoạt động'}))}).success).toBe(false);});});
